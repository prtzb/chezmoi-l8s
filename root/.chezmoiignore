# Ignore files that shouldn't be applied to all nodes
{{- if not .is_control_plane }}
etc/haproxy/
etc/keepalived/
{{- end }}

{{- if not .is_bootstrap }}
scripts/bootstrap-cluster.sh
{{- end }}

{{- if not (and .is_control_plane (not .is_bootstrap)) }}
scripts/join-control-plane.sh
{{- end }}

{{- if not .is_worker }}
scripts/join-worker.sh
{{- end }}

# Always ignore these
README.md
CLAUDE.md
.git/
.gitignore
.chezmoiroot