# Ignore files that shouldn't be applied to all nodes
{{- if eq .node_type "agent" }}
etc/haproxy/
etc/keepalived/
{{- end }}

{{- if not .bootstrap }}
scripts/bootstrap-cluster.sh
{{- end }}

{{- if not (and (eq .node_type "server") (not .bootstrap)) }}
scripts/join-control-plane.sh
{{- end }}

{{- if not (eq .node_type "server") }}
scripts/join-worker.sh
{{- end }}

# Always ignore these
README.md
CLAUDE.md
.git/
.gitignore
.chezmoiroot