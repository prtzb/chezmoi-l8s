network:
  version: 2
  renderer: networkd
  ethernets:
    eth0:
      dhcp4: false
      addresses:
        - {{ .node_ip }}/24
      routes:
        - to: default
          via: {{ .network.gateway }}
      nameservers:
        addresses:
          - {{ .network.dns }}
        search:
          - local